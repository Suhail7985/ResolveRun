# ResolveRun web (Next.js)

This is the **ResolveRun** frontend. Run it from the repo root:

```bash
npm run dev:web
```

Open [http://localhost:3000](http://localhost:3000).

Public deploy: Vercel, root directory `frontend`. See [DEPLOY.md](../DEPLOY.md).
